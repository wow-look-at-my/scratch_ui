{
	"//": "Editors, and the `typecheck` script. ts0 generates its own tsconfig to type-check what it builds (src/components + what it imports); this mirrors it and additionally covers the build scripts, which run under Node's type STRIPPING — stripping never type-checks, so without this pass a type error there would only surface as a runtime failure.",
	"compilerOptions": {
		"target": "ESNext",
		"module": "ESNext",
		"moduleResolution": "bundler",
		"lib": ["ESNext", "DOM", "DOM.Iterable"],
		"strict": true,
		"noEmit": true,
		"skipLibCheck": true,
		"allowImportingTsExtensions": true
	},
	"include": ["src/**/*.ts", "css.d.ts", ".github/scripts/**/*.ts"]
}
