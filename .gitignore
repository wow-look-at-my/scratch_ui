node_modules/
# No lockfile is committed: ts0 is a branch dependency and every install
# resolves it to current HEAD (same as js-snippets).
package-lock.json
pnpm-lock.yaml

# Compiled components (generated by `ts0 build` from src/components/)
dist/

# Assembled site output (generated by .github/scripts/assemble-pages.ts)
_site/
