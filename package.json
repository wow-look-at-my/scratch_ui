{
  "name": "scratch-ui",
  "version": "0.2.0",
  "description": "Scratch Proto design language — design tokens + native web components",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/wow-look-at-my/scratch_ui.git"
  },
  "type": "module",
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "build": "ts0 build --config ts0.scripts.json && node dist-scripts/build-css.js && ts0 build",
    "site": "pnpm build && node dist-scripts/assemble-pages.js"
  },
  "files": [
    "dist",
    "src/scratch-ui.css"
  ],
  "exports": {
    ".": "./dist/scratch-ui.js",
    "./scratch-ui.css": "./src/scratch-ui.css"
  },
  "devDependencies": {
    "@types/node": "^22.10.0"
  }
}
